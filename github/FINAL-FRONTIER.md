# Exact-candidate implementation — 2026-09-20

Implementation source: research commit `41ce845ed61b1a442ad548d91e9ca75d414496d3` on `claude/merge-proof-deep-research-czjemw`; charter §43, with Ryan's canonical verdict decision and September 19 GitHub-policy addendum. The implementation branch is `codex/final-frontier-l3`. Current product candidate: `7492959d9ae5ef5bb9c889edb3bcfe3b410d8f69`. The pre-addendum Level 3 baseline is `a6800e6`; the candidate changed because product code changed; later documentation commits do not change this identity. Level 3 remains open at live-validation and owner gates. This document describes a local candidate, not a production release. See the [complete productionization evidence](validation/final-frontier-l3-2026-09-19/productionization.md) and [22-item charter coverage](validation/final-frontier-l3-2026-09-19/charter-coverage.md).

## What the candidate does

The existing GitHub collector and deterministic engine now bind the candidate's repository, commit, tree, base and group to check App/workflow identity, workflow blob, qualifying event, PR context, run attempt, job and steps. Same-name workflow substitution and wrong-event substitution cannot establish execution. Changed producer workflows, unsupported producers and missing metadata remain explicit gaps. Workflow `uses:` pinning observations are advisory; no workflow source or logs are retained.

`VERIFIED` requires sufficient current positive evidence. `FAIL` requires a demonstrated unmet required condition with the necessary bindings. Missing, ambiguous, stale, unsupported or unavailable evidence yields `NOT_PROVEN`. The local CLI uses `collectionComplete: false` for collection failures; historical records keep their recorded verdict. Factory fulfillment still rejects incomplete collection.

Currentness compares dependencies for TARGET, CI_EXECUTED, APPROVAL_CURRENT, RULES_SNAPSHOT and REMOTE_DURABLE. Unrelated checks, comments, mergeability rollups and push-time rules do not invalidate them. Signed relevant events trigger targeted re-observation with conditional reads; they do not pretend the dependency has already changed. Six-hour complete reconciliation and four-hour App-delivery scans recover missed events. Receipt bodies and observation identifiers remain immutable; new receipts link to predecessors. Unchanged full reconciliations retain the receipt and refresh currentness; they do not consume receipt capacity. Existing report-only defaults, tenant boundaries, pricing and metering authority remain intact.

Approval evidence records opinionated writer reviews, permission at observation, account class, dismissal history and head-push activity. Bot reviews never count as human approvals. Bot authors add one approval; the initiating human must be established before the authorization claim can pass. A push actor is not assumed to be an agent initiator. CODEOWNERS, last-push satisfaction and unattributable initiators remain named limitations.

Merge events append landed observations joined to immutable pre-merge receipts. A different squash commit can match by tree and parent; different content is a mismatch, and unsupported parentage is unresolved. Rule-suite results record observed bypasses without claiming visibility into exempt actors. Push ranges record commits without proof. The existing merge-history list shows the landed state; lookup by commit returns the same evidence.

Independent reconstruction runs in an isolated worker against an operator-owned bare blobless mirror, with Git version and binary SHA-256 pins, fixed merge options, empty attribute source, no replace refs, no hooks and no configured custom merge drivers. Nonempty mirror `info/attributes` is rejected; system/user/local attribute-file overrides are disabled. Clean merge/squash, confirmed ordered queue entries and first-parent rebase are supported. Conflicts, both-sided gitlink changes and rebase merge commits refuse a tree. Attribute, multiple-base, rename, case, empty-commit and duplicate-patch caveats are retained; caveated divergence never becomes a definitive mismatch.

Portable bundles contain the immutable receipt, normalized/raw policy snapshot, explicit claims and observation references, verdict-info, in-toto statement and DSSE envelope. Verification checks digests, exact subjects and deterministic replay, with frozen versioned logic. Signatures require an independently supplied trusted P-256 key; a bundled key does not authenticate itself. Unsigned output is labeled and rejected unless the verifier caller explicitly accepts unsigned consistency checking. With `--git-dir`, the verifier uses caller-supplied bare Git objects and the recorded binary pin to independently check trees, merge-base/parent/ancestry relationships and the expected tree. Missing objects remain unavailable; lazy fetching is disabled and no refs are updated. Reconstruction may add synthetic objects to that local mirror. Online checks distinguish immutable-record divergence, mutable-state changes and missing records. Provider assertions remain provider-trusted. The local operator log is a hash chain with signed cumulative daily Merkle roots and inclusion proofs. An explicit owner-operated publisher verifies the approved signature, repository ownership and immutable existing bytes before publication. No root has been published; inclusion alone does not establish external witnessing.

## GitHub September addendum

Repository Actions policies, including inherited policy sources, feed the existing producer and policy model. Active policies bind exact workflow path, actor, triggering actor, event and provider policy revision time. A policy revision at or after execution cannot prove historical compliance. Known actor/event violations by an otherwise bound producer are FAIL; unknown membership, scope, history or rerun-actor eligibility is NOT_PROVEN. Fine CI claim bindings preserve unrelated workflow claims when only one policy changes. Disabled, evaluate-only and unrelated workflow policies are omitted from receipts and dependency hashes. The product does not configure these controls.

GitHub documents Administration **write** for policy reads. The App continues to request Administration read. Missing discovery or an unreadable applicable policy now blocks positive execution proof; it is no longer merely listed as not checked. This does not authorize an App permission increase. Existing authorized bearer credentials can perform readable observations. Policy event hints trigger re-observation, and complete reconciliation remains necessary where GitHub delivers no event.

`code_coverage` ingestion retains source identity and minimum/drop parameters. The bound aggregate evaluator checks repository, PR, head, exact candidate commit/tree, complete line counts, linked producer/workflow/blob/event/run/attempt/job, and the current default-branch baseline when a drop threshold applies. Decimal thresholds use exact rational comparisons; drop means percentage points; GitHub documents zero as disabling that threshold. A demonstrated bound violation is FAIL, an eligible satisfied requirement is positive, and incomplete/ambiguous/unavailable evidence is NOT_PROVEN. Coverage policy changes invalidate coverage and policy claims while preserving unrelated CI/approval claims.

**Provider evidence boundary:** the positive aggregate input is a normalized internal evidence contract exercised by fixtures, not a published GitHub REST response schema. The documented APIs expose coverage thresholds and upload processing status, but not a complete aggregate with the required candidate/producer provenance. The official upload action uses PR heads and skips merge-group uploads. The live collector explicitly returns `GITHUB_COVERAGE_BOUND_AGGREGATE_API_UNAVAILABLE`; it does not parse comments, invent an endpoint, accept arbitrary uploaded metrics, or equate green CI with coverage. The positive live collection adapter remains unimplemented pending sufficient provider evidence. This is an open addendum item, not a removed capability or a claim of Level 3 completion.

Portable receipts retain only policies and coverage provenance that contribute to their claims. Frozen verifier v6 records this engine; v2–v5 remain immutable for historical replay. Online reverification also checks actor/attempt identity and relevant current execution policies. Provider observations remain GitHub-trusted, distinct from independently recomputed Git content.

Sources: [Actions policy API](https://docs.github.com/en/rest/actions/policies), [execution protection controls](https://docs.github.com/en/actions/how-tos/administer/control-workflow-execution), [coverage rule API announcement](https://github.blog/changelog/2026-09-18-manage-the-code-coverage-ruleset-condition-with-the-rest-api/), [coverage threshold semantics](https://docs.github.com/en/code-security/how-tos/maintain-quality-code/restrict-code-coverage), [coverage API surface](https://docs.github.com/en/rest/code-quality), [official upload action](https://github.com/actions/upload-code-coverage). See [addendum acceptance](validation/final-frontier-l3-2026-09-19/capability-addendum.md).

## Using the candidate

The machine contract is `urn:merge-proof:decision:1`. `POST /proof/v1/decision` requires a GitHub bearer token and exact repository ID, PR, expected head/base/target SHAs. Hosted use additionally requires access to the App installation and its repository. CLI/MCP validate the full response, caller bindings and consistent outcome/verdict/currentness before consuming it. Output is PROCEED, HOLD, REFUSE or UNAVAILABLE, with the unchanged three-verdict vocabulary, currentness, bindings, reasons, next action and receipt. PROCEED is a point-in-time observation. A caller must still use GitHub's head-SHA guard and required checks; the base is not reserved. Queue admission alone cannot authorize merging.

```sh
merge-proof verify --repo OWNER/REPO --repository-id ID --pr NUMBER \
  --head HEAD_SHA --base BASE_SHA --target TARGET_SHA --json
# MP_GITHUB_TOKEN supplies the existing token. MP_ORIGIN selects the service.
# --wait SECONDS permits bounded polling; neither command merges.
gh merge-proof verify --help
merge-proof verify --bundle DIRECTORY --trusted-keys trusted-jwks.json
merge-proof verify --bundle DIRECTORY --allow-unsigned
merge-proof verify --bundle DIRECTORY --trusted-keys trusted-jwks.json --git-dir BARE_REPO
merge-proof mcp
```

The bundle endpoint is `GET /proof/receipts/RECEIPT_ID/bundle`; authorization is rechecked. It returns a JSON bundle. Save it as `bundle.json` in the offline directory, or use `bundle.write` to write the component files. `GET /proof/v1/receipts?repository_id=ID&commit=SHA` joins artifact commit identity to recorded landing evidence. Missing/retention-expired records do not invalidate an otherwise internally consistent historical statement.

Agent instruction example: “Call merge_proof_decision with the exact repository and expected SHAs. Only PROCEED permits continuing the existing merge procedure. Preserve the returned head-SHA guard and required checks. HOLD, REFUSE, UNAVAILABLE, malformed responses and tool errors require stopping the merge step. The tool does not merge.”

`github/verifier/freeze.js` is a release-time generator for this unpublished policy archive. After publishing an archive, preserve it permanently and mint a new directory/table entry for changed logic. Do not regenerate a published version. Old v1 receipts remain readable but are explicitly unsupported by the new portable replay protocol; they are not silently reissued as v2.

## Operational settings and limits

The optional App configuration `reconstruction` takes `root`, absolute `binary`, exact `version` output and `sha256`. Use a dedicated operator-controlled directory. The token is supplied only to the worker's Git environment and is not persisted. Missing configuration produces `MIRROR_NOT_CONFIGURED`. A pinned static Linux Git 2.50.1 core and HTTPS helper have been built, tested against the final 20 reconstruction/chain cases, and retained as an exact artifact; see [runtime provenance](validation/final-frontier-l3-2026-09-19/linux-runtime.json). The [final 20-test recheck](validation/final-frontier-l3-2026-09-19/linux-final-reconstruction.json) names the current reconstruction source. The final production-host installation and acceptance remain owner gates. Source/image pins plus recorded dependencies do not assert byte-reproducible rebuilding.

The collector now reads a complete provider-reported queue prefix, bounded to 100 entries, and validates repository, PR heads, contiguous positions and predecessor candidate/base links before reconstruction. Every reconstructed step is compared with its exact provider candidate Git tree; a mismatched intermediate step cannot be hidden by matching final content. A later entry binds to its validated predecessor for landed parentage. Missing/partial/ambiguous order still yields `QUEUE_MEMBERSHIP_ORDER_UNAVAILABLE`; branch naming is not membership evidence. The positive signed group-to-landed journey on the final code remains pending owner selection of the isolated development-App fixture. ALLGREEN sibling validation, complex rebase/indirect landings, private provider merge configuration, CI checkout behavior, permission at approval time, exempt bypasses and atomic merge-time authorization remain explicit limits.

Persistent stores automatically write an immutable fsync-backed sidecar archive for receipts, landings and merge identity/commit indexes. A bounded active cache can evict old records without losing authorized historical downloads or commit lookup. Restored history never becomes CURRENT by restoration. Back up the archive with the state snapshot; local durability is not replicated storage or disaster-recovery proof. Failed saves retain pending durability and cannot permanently lock the worker. Retracted checks for closed/untracked PRs remain queued durably until their provider updates succeed. Delivery scans and reconciliation are bounded and cannot recover permanently unavailable evidence.

The implemented AWS KMS P-256 signer checks the exact key ARN, algorithm and returned signature against an independently configured public JWKS; configured failure never falls back to unsigned issuance. Public JWKS is available without a customer login, while bearer bundle download and archived UI access still recheck installation/repository access. Key selection, live KMS verification and independent publication/mirroring are owner gates. See [operator instructions](operations/README.md).

## Remaining owner and environment gates

- Complete [GitHub owner confirmation](https://github.com/organizations/ohcaygo/settings/installations/160648161) and add only `ohcaygo/merge-proof-l3-lab-queue-headgreen` to development App installation `160648161`. Public organization queues are available without a paid upgrade. App permissions already include Merge queues read and merge_group events. The normal selection API returned 403; positive signed queue/currentness/landing acceptance is not yet proven.
- Select an existing Team/Enterprise test organization or explicitly authorize a paid plan change for organization-wide ruleset enforcement. Repository-level rules and Actions policies were exercised separately. Public push-rule restrictions and unresolved workflow/license-rule API availability remain named gates, not capability removals.
- Choose the production KMS/JWKS identity, public daily-root destination and publication authority, persistent storage/runtime, and production deployment. Production credentials, public anchoring, spending and rollout remain unexecuted owner decisions.
- Select the private non-production scheduler/credential identity for weekly differential-lab activation. Drivers, bounded observation/comparison and runtime drift metrics are implemented; no recurring external job has been activated.

An earlier orphan-branch queue attempt in the product repository was observed by the production App and caused a neutral check on synthetic PR 16. The PR was closed unmerged, dequeued, and its synthetic ruleset disabled; main remained unchanged. No deployment occurred. Further signed queue work uses only the dedicated synthetic repository. Full disclosure and exact evidence are in [productionization](validation/final-frontier-l3-2026-09-19/productionization.md).

## Validation and review

The pre-addendum code passed 314/314 GitHub tests in 98.4 seconds locally, including the real-service world/fetch model's 1,000 sequences, 28,578 actions and seventeen invariant obligations, plus real stdio MCP fixture-agent acceptance. The model is bounded generated testing, not exhaustive formal verification. A 30-PR/200-event budget fixture used 300 counted reads (11,350 network requests including conditional reads). The final 132-file npm artifact passed trusted historical bundle verification, independent Git recomputation, MCP and gh-shim smoke checks.

Candidate `6fc94f2` passed all 11 live assertions on the unchanged non-queue path from expected-tree matching through machine decision, guarded synthetic squash merge, landed-content comparison, archive eviction/restart and signed portable verification with independent Git objects. These used an ephemeral fixture signing key and live REST/GraphQL evidence; they do not establish production KMS or signed merge_group-to-landed acceptance. Existing separately recorded approval, permission-loss and signed App recovery evidence retains its exact scope. Final candidate `a6800e6` additionally passed [live ordered-queue acceptance](validation/final-frontier-l3-2026-09-19/ordered-queue-final.json): exact prefix, current VERIFIED, both intermediate tree comparisons, landed content/parentage, trusted portable chain and independent offline recomputation. Signed queue webhook propagation remains the owner-gated part.

The intermediate complete candidate `d162d377` received one independent review: NEEDS_FIXES, four P1 issues. Queue predecessor binding, unresolved landing history, failed-save retry and hosted JWKS/bundle routing were repaired and regression-tested. The model/fixture-agent acceptance gaps were implemented; the model exposed and led to fixes for two stale-check retraction defects. No independent re-approval of the final commit is claimed.

Local CLI/report tests passed 26/26 and 9/9. The latest unchanged factory suite was 36/37 because the local Chromium PDF process aborted; prior green runs are not substituted for that failure. Production-host PDF acceptance remains unproven. Exact outputs, live boundaries, provider limitations and all charter items are recorded in [productionization evidence](validation/final-frontier-l3-2026-09-19/productionization.md). Older reports preserve their original candidate identities and outcomes.
