# Level 3 productionization evidence — 2026-09-20

**Outcome: final-candidate isolated signed queue acceptance passed; Level 3 remains OPEN at provider, implementation and production owner gates. Not deployed.**

The current [signed queue acceptance](signed-queue-final.md) supersedes historical pending queue statements below. Product candidate 7953ecd passed 13 real signed-delivery-to-landed assertions, independent Git replay and archive restart under a temporary one-repository development-App Write token. Administration is restored to Read. Ryan selected Read as the production default with optional Enhanced Policy Proof. The [focused least-privilege investigation and design](least-privilege.md) found no equivalent documented narrower source. Its dedicated credential integration/customer opt-in and the positive coverage aggregate adapter remain unimplemented. The [production acceptance packet](../../operations/LEVEL3-RELEASE-PACKET.md) prepares signing, independent trust, anchoring, storage recovery, deployment and scheduled lab operation; none was activated. Later sections retain explicitly historical pre-addendum/resume evidence.

Current product candidate: **`7953ecdbd849c8afe0379bfec1b4a61133292c62`**. The [capability addendum report](capability-addendum.md) records its current acceptance and remaining implementation gap. The historical pre-addendum evidence below belongs to baseline **`a6800e68f4c1e03d0cd0de28fe7febe5864bcdc4`**, which superseded `e3984f6`. `d162d37727876ea259a407edb42914012483ddcd` was the intermediate independently reviewed candidate. Later documentation commits do not replace product identity. Remote product `main` remains `e76a8677aa3dfd935376981ad4b99d3444e0441d`; the candidate has not been integrated or deployed. Exact source snapshots were exported only to the owned synthetic acceptance fixture.

Authority remains the three research passes and [Level 3 §43](../../../docs/research/MERGE-PROOF-FINAL-FRONTIER-L3-2026-09-19.md), including Ryan's canonical verdict decision. No remaining validation limit changes the product frontier. Sufficient current positive evidence is required for VERIFIED; unavailable or ambiguous evidence remains NOT_PROVEN; a demonstrated unmet required condition is FAIL.

## 1. Implemented and live-validated

Candidate `6fc94f2` completed the full supported non-queue path (unchanged by the final queue-only reconstruction correction) on [owned synthetic rules PR 12](https://github.com/ohcaygo/merge-proof-l3-lab-rules/pull/12): EXPECTED matched PROVIDER CANDIDATE, bound CI/policy evidence yielded VERIFIED, the normal HTTP decision endpoint returned PROCEED with the exact head guard, a wrong head returned REFUSE, the guarded squash merge landed matching content, historical currentness became STALE, commit lookup resolved the landing, archive eviction plus actual store restart recovered the historical receipt without restoring CURRENT, and the signed portable chain verified with independently supplied fixture trust and Git objects. **11/11 assertions passed.** Required approval count was zero for this fixture; positive approval/revocation and permission-loss evidence is the separately preserved prior live acceptance, not a new claim of independently human review in PR 12.

Receipt `f638c6dc-f868-40c4-b339-a91f939c4e22` records source commit `6fc94f22e28d9780fc3d9f8fb9a8829dda45f83f`; landed commit `8d87dc2bcf71341ae529d15c1a00205bd42bd993`. Its policy code digest is `bd1d7b8d5cf6e92c61a112a94bf3b0146ca69d8b26834186673f7548474fa145`, preserved as verifier archive v4. See [machine-readable final journey](portable-live-final.json). Signing used an ephemeral local P-256 fixture key, not production KMS. This journey used real GitHub REST/GraphQL records and local service orchestration; it does not claim signed merge-webhook delivery. Currentness at the atomic instant of merge remains UNAVAILABLE. No completed-day operator checkpoint was included in this same-day receipt. [Online verification](portable-online-final.json) subsequently matched the retained check, attempt, job, workflow blob, commit, rule and landed records; the moved base ref was correctly EXPECTED_CHANGE, yielding PARTIALLY_REVERIFIED rather than claiming historical state is still current.

The earlier PR 11 run initially refused independent verification because the offline mirror lacked the landed commit object. Fetching that exact object recovered exit 0. The refusal and correction remain in the private evidence; PR 12 repeated the complete corrected journey on product commit `6fc94f2`. The final `a6800e6` change adds intermediate queue-candidate comparisons and preserves the non-queue path.

The earlier live matrix remains applicable where behavior and conditions are unchanged: classic/repository rules, merge/squash/single-commit rebase trees, HEADGREEN API-observed groups and rebuilding, base/head movement, producer/event/workflow substitution, failed checks, forks, 22 runs per SHA, bypass/exempt observations, real stale-proof landing mismatch, direct pushes without proof, ETags, CLI/MCP, account approval/dismissal, and observed reviewer Write→Read→Write changes. See [initial lab](live-lab.md), [App delivery recovery](app-resume.md), [reviewer behavior](reviewer-resume.md), and [permission loss](permission-resume.md). Same-operator accounts do not establish distinct human principals. These are bounded cases, not universal provider coverage.

### Final ordered queue acceptance

Final product `a6800e68f4c1e03d0cd0de28fe7febe5864bcdc4` then passed the real provider-ordered queue path on [synthetic PR 8](https://github.com/ohcaygo/merge-proof-l3-lab-queue-headgreen/pull/8), after PR 7. The collector obtained the full two-entry prefix and exact predecessor/base relationship; the normal service produced CURRENT VERIFIED with no gaps; both independently reconstructed intermediate trees matched their exact provider candidate objects; the provider landed matching tree and parentage; and the signed portable landed chain verified. Five live assertions and the packaged offline Git verifier passed. Its additional offline rows independently matched both QUEUE_CANDIDATE_TREE and QUEUE_EXPECTED_STEP pairs. [Complete normalized evidence](ordered-queue-final.json).

A third synthetic entry released the held batch without weakening required checks. PRs 7, 8 and 9 merged only into the dedicated synthetic repository and its original rules were restored. This is real REST/GraphQL and Git-object acceptance with an ephemeral signing key, not signed-webhook propagation. An earlier re-observation during queue transition correctly returned STALE/NOT_PROVEN; that negative result is retained privately. The final controlled run established the positive current path on `a6800e6`. The earlier `6fc94f2` two-entry run also passed final-tree/landing/portable checks but lacked per-step comparisons, which is why the final run was necessary.

### Repository rules and Actions policies

The expanded matrix performed 40 initial cases: 33 expected assertions passed and seven initial payloads were rejected. Corrected payloads then established deployment configuration with a real environment, tag rules on the correct target, and the genuine push-rule environment restriction. Do not count malformed initial requests as product/provider limitations. Full results: [repository policy matrix](repository-policy-matrix.json).

| Repository-level path | Live result and meaning |
| --- | --- |
| Required check baseline; zero required reviews | VERIFIED with exact eligible evidence |
| Creation, deletion, non-fast-forward, update, branch pattern, commit message and author/committer email patterns; Copilot review request | Recorded push-time policy; did not invent evidence obligations |
| Tag rule | Created on tag target; correctly excluded from branch proof; temporary rule deleted |
| Linear history, signatures, deployment, code scanning, review-thread resolution, Code Quality | Active configuration observed; unsupported required evidence remained NOT_PROVEN. This validates refusal, not positive satisfaction |
| CODEOWNERS and last-push approval | Named NOT_PROVEN boundaries retained |
| Positive required review count without approvals | NOT_PROVEN; earlier separate live approval/permission tests cover positive and revocation behavior |
| Coverage minimum, maximum drop, both together | Exact thresholds retained; unavailable bound Code Quality evidence remained NOT_PROVEN |
| Disabled, excluded or evaluate ruleset as only check source | NOT_PROVEN: no independent required validation configured. Accepted evaluate mode is not enforced mode |
| Actions event allow/deny | Allowed bound run VERIFIED; denied event NOT_PROVEN |
| Actions disabled/evaluate/path-excluded/nonmatching | Correctly non-enforcing for the observed candidate |
| Actions exact actor, unmatched actor, unobservable role | Exact actor passed; unmatched or unknown membership remained NOT_PROVEN |

Repository ruleset `23701774` and Actions policy `4836` were restored. PRs 9 and 10 were closed. Temporary tag ruleset `23717638` was deleted. The owned synthetic deployment environment remains available for retesting. No production repository policy was used for this matrix. Administration-write requirements for Actions policy reads are still explicit: the product App retains Administration read and records read denial as unavailable; the lab used already-authorized owner credentials. Product App scope was not weakened or broadened to make the proof pass.

### Static Linux reconstruction artifact

[Owned synthetic Actions run 35489128375](https://github.com/ohcaygo/merge-proof-l3-lab-classic/actions/runs/35489128375) built Git 2.50.1 and its HTTPS helper statically in a pinned Alpine image, verified no ELF interpreter/shared-library dependencies, successfully fetched from the owned public fixture over TLS, and passed **19/19 Git reconstruction/receipt-chain corpus tests**. The core executable SHA-256 is `3d2b9bc579af799be2f88ed81c01cec14e4f469dbf6fef377322c29710e66d2b`; HTTPS helper SHA-256 is `f5b324107f840a22e82440e0c1bbeb6687da6f481142d5fec6fbc03085478c23`.

[Runtime provenance](linux-runtime.json) names the fixture commit and exact published corpus revision `e93c94b36433e195fb84f4b7b96ea6b8dc93873e`; reconstruction source was unchanged. This is not a claim that every final-candidate service test ran on Linux. Source and builder image are pinned; dependency versions and output hashes are retained, not a claim of bit-for-bit reproducible rebuilding. The libidn2 static archive's `error` symbol is renamed in the disposable builder to avoid collision with Git; before/after hashes are captured. Artifact, upstream licenses, package list and logs are retained privately outside the public product tree. Production must install and test the same artifact under owner authority. A [second Linux run](https://github.com/ohcaygo/merge-proof-l3-lab-classic/actions/runs/35490087078) reused and rehashed the retained static binary and passed **20/20** reconstruction/chain tests with the exact `a6800e6` reconstruction source and current test file overlaid on the published corpus. [Final source/runtime provenance](linux-final-reconstruction.json) and [output](linux-final-reconstruction-tests.txt) preserve that precise scope.

## 2. Implemented, with provider/environment validation limits

| Environment/API gate | Current evidence | Minimum next environment |
| --- | --- | --- |
| Organization-wide ruleset enforcement and inheritance | Local collector/currentness/unsupported-policy tests implemented; owned `ohcaygo` organization is Free. No live org enforcement claim | GitHub Team or Enterprise organization and org-owner access. Team is the minimum documented plan; no purchase made |
| Repository push rules: path, path length, extension, size | Four corrected push-only requests each returned 422, `Source public repos cannot have push rules` | Eligible private/internal repository under the documented paid-plan rules. Public test repo cannot validate this path |
| Required-workflow repository rule | Corrected request with pinned workflow source returned 422, `Workflow error at index 0` | Eligible provider feature/source setup. Exact cause is not proven to be plan-only; do not assert a paid upgrade alone fixes it |
| License-compliance rule | 422 invalid rule type in current account/API environment | Provider support for this rule; feature/plan cause remains UNKNOWN |
| Positive coverage, CODEOWNERS, last-push attribution and unobservable role membership | Explicit conservative refusal implemented and exercised | Exact bound provider evidence where GitHub exposes it. No fabricated endpoint or inferred success |
| ALLGREEN sibling evidence; ambiguous/incomplete queue order; complex rebase shapes | Refusal behavior implemented; existing local/live negative cases pass | Sufficient exact evidence or a reconstructable history. These documented epistemic limits are preserved |

GitHub distinguishes [repository rulesets](https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-rulesets/about-rulesets) from [organization rulesets](https://docs.github.com/en/organizations/managing-organization-settings/creating-rulesets-for-repositories-in-your-organization). Organization rulesets require Team or Enterprise. [Merge queues](https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/configuring-pull-request-merges/managing-a-merge-queue) are available to public organization-owned repositories without buying that plan; private organization queues require Enterprise Cloud. [Rules REST API](https://docs.github.com/en/rest/repos/rules) and [Actions policies API](https://docs.github.com/en/rest/actions/policies) supply the policy shapes used here. A plan/API refusal is not a product capability removal.

### Historical pre-elevation resume — superseded by accepted signed queue evidence

The minimum dedicated fixture is public, owned `ohcaygo/merge-proof-l3-lab-queue-headgreen`, repository ID `1377343185`, queue ruleset `23701853`. Development App `4899448`, installation `160648161`, retains merge_group subscription, Merge queues read and unchanged permissions. The approved pair is now saved: the product repository plus only this queue fixture.

The [owner-resume preflight](signed-queue-owner-resume.md) replaced the prior installation 404 with HTTP 200 and obtained a repository-scoped token (HTTP 201). The required execution-policy read then returned **403 / Resource not accessible by integration**, with GitHub's **`administration=write`** accepted-permissions header. Administration remains read. Under the current explicit no-expansion instruction, this is a provider/permission validation gate. No selection or permission change is requested again, and no policy evidence is inferred or substituted.

The isolated runner remains pinned to product `7953ecd`, regardless of documentation HEAD. It checks installation and policy-read access before fixture writes. The full signed delivery → exact group → evidence/currentness → destruction/rebuild → landed content → portable chain acceptance did not start on this resume and remains **NOT_PROVEN**. No new PR, ruleset, check, queue mutation or merge was created.

Real development-App `merge_group` redelivery was separately verified: provider delivery ID `3843728875423006720`, GUID `45856690-b4a5-11f1-9ef6-2784255486a2`, raw SHA-256 `00822a9ade99d3b253a7e170ecaedc1b2b996b91544966319bba1982234a2167`. The normal HMAC handler accepted the exact candidate, persisted the group queue and identified a duplicate. It issued zero receipts. This closed-fixture observation proves transport recovery and exact ingestion only; it cannot close the positive current queue/landed gate.

**Disclosed production side effect:** an earlier attempt used an orphan fixture branch in the already-selected product repository. Production App `4901537` observed synthetic PR 16 and emitted a neutral check. That was not the intended isolation boundary. PR 16 was closed unmerged, dequeued, and only synthetic ruleset `23717603` was disabled. Product main stayed unchanged. No deployment occurred. Do not describe this turn as having had no production interaction, and do not reuse that fixture for further testing. Raw signed webhook material stays private.

## 3. Production owner gates

1. **Signing and trust:** select the existing or approved new KMS P-256 key ARN, region, runtime IAM identity, trusted JWKS identity/publication path and historical key mirror. The operational AWS KMS adapter verifies every returned signature and fails closed; only simulated KMS and ephemeral live-lab signatures have been tested. Production KMS call and customer trust bootstrap remain NOT_PROVEN.
2. **Public anchor:** select the owned public repository/branch and authorize initial signed-root publication. Signed cumulative Merkle checkpoints, inclusion proofs and an owner-operated immutable publisher are implemented/tested. No public root has been published. Offline inclusion never fabricates external witnessing or a timestamp.
3. **Production runtime/durability:** authorize the release host/storage, install the tested static Git artifact, configure persistent archive backup/capacity monitoring, and run release preflight plus hosted acceptance with production credentials. Local fsync/restart is not replicated-storage or disaster-recovery proof.
4. **Release:** authorize production integration/deployment only after the remaining acceptance gates. No production rollout, billing change or new paid service was performed here. [Final owner-gate readback](owner-gates-readback.json) confirms unchanged development-App/installation permissions, Merge queues read, merge_group subscription, Free organization plan and unchanged product main.
5. **Ongoing private lab:** select the non-production scheduler/credential identity and authorize weekly activation. The allowlisted read-only comparison runner, private fixture drivers, fixture-diff alarms, reconciliation-supersession metric and rule-suite disagreement metric are implemented; recurring external execution has not been activated. It must never target product/customer repositories or the production App.

The signing/publication choices were asked while other authorized work continued. The organization-plan and production activation decisions remain pending. Dedicated App selection is complete. Read is the selected production default; optional Enhanced Policy Proof requires the separately documented implementation, registration and customer authorization. The earlier temporary lab grant is restored and supplies no production authority.

## 4. Charter implementation accounting

See [all 22 numbered §43 items](charter-coverage.md). **No mandatory pre-addendum code item in §43 is knowingly left unimplemented. The current capability addendum still has an unimplemented positive coverage collection adapter, explicitly recorded in category 4 of its report.** This is code coverage, not a declaration that Level 3 acceptance is complete: signed queue positive acceptance is now closed; organization-only enforcement, production signing/trust, public anchoring and production-host validation remain open. The subsequently designed Enhanced opt-in integration is also unimplemented.

The optional checkout-log INFER parser in §43.1.6 is not implemented. GitHub-token authentication implements the explicit “GitHub-token or Actions OIDC” alternative in §43.5.1; the OIDC alternative is not implemented. The second-phase Quint specification discussed in §21 is also not implemented and is outside §43's selected node:test harness. These omissions are stated rather than silently relabeled as implemented. No AI verdict, auto-merge, deployment system or other prohibited product expansion was added.

## Pre-addendum tests and independent review

- **314/314 GitHub tests** passed on final product code; full run 98.4 seconds on Node 24.12.0/macOS. [Complete output](productionization-github-tests.txt).
- The real-service world/fetch model passed **1,000 sequences, 28,578 actions, 28,600 deliveries, 949 duplicates, 705 injected provider faults**. It combines model assertions with the existing exact-binding/landing/crypto corpus for the seventeen invariant obligations; this is bounded generated testing, not an exhaustive formal proof. [Model report](service-model.json). Normal CI includes this suite; the new harness had not yet run in hosted CI at this pre-addendum checkpoint. Current hosted validation is recorded in the capability addendum.
- The 30-PR/200-event fixture consumed **300 counted reads**, below the charter's 2,000 ceiling. It made 11,350 actual network requests because conditional 304 reads are separately counted. The measured unconditional full-rescan baseline was 1,440 per 30-PR scan, extrapolated to 288,000 for 200 events. Do not describe the extrapolation as observed production traffic. [Budget methodology/results](service-model-budget.json).
- The fixture agent used a real stdio MCP tools/call → HTTP service path. Only exact validated PROCEED invoked its synthetic head-guarded merge; HOLD, FAIL/REFUSE, wrong head, malformed response and tool error stopped; a head race was refused. The tool has no merge action.
- Final npm artifact: 132 files; trusted historical bundle replay, independent Git verification, read-only MCP discovery/output schema and gh shim passed. [Artifact smoke](productionization-package.json). v2/v3 verifier archives were preserved; v4 and v5 were added.
- [Unchanged local CLI/report suite](productionization-local-tests.txt): 26/26 and 9/9 passed in this work. [Pre-addendum local factory run](productionization-factory-tests.txt) was **36/37**, with the real PDF journey failing because local Chromium aborted (SIGABRT; crashpad/database and filesystem diagnostics). No factory code changed. Do not report the prior 37/37 as this run's result. The production-host PDF check remains unproven here.

One independent review of complete intermediate candidate `d162d377` returned NEEDS_FIXES with four P1 defects: later ordered queue base/landing binding, unresolved-then-resolved portable landing history, failed durable-save retry, and hosted public JWKS/bearer archive routing. All were repaired and covered by regressions. Its P2 model/fixture-agent acceptance gaps were also implemented. The model then found stale published success after PR close and group selection changes; durable retraction handling was repaired. A transient save can no longer permanently hold the drain lock. The final ordered-queue inspection identified missing intermediate provider tree comparisons: `a6800e6` fetches every exact provider candidate, compares every reconstructed step, carries candidate/tree/comparison in the receipt, refuses a demonstrated intermediate mismatch even if final content matches, and independently rechecks those objects offline. A regression reproduces that otherwise-hidden mismatch. No false VERIFIED was reproduced in the exercised final cases. The reviewer did not independently re-approve the final commit; passing tests are not represented as that approval.

Private evidence root: `merge-proof-l3-live-lab-2026-09-19/frontier-resume/`. Calibration captures, raw signatures, state snapshots, runtime artifact and fixture scripts remain there. The public report contains summaries and provenance only. All active local receivers were stopped. [Final fixture readback](final-cleanup-readback.json) confirms zero open PRs and zero in-progress workflows across the four owned repositories. Synthetic merged fixtures, branches and runtime artifacts are retained for inspection; fixture policy cleanup is recorded. Remaining uncertainty stays visible.


## September 20 capability addendum

The pre-addendum product baseline remains `a6800e6`. Addendum proof candidate `7492959d9ae5ef5bb9c889edb3bcfe3b410d8f69` strengthens execution-policy discovery/binding/currentness and coverage-rule evaluation. Intermediate `470f679` repairs owned browser descendant cleanup found during complete factory validation. `8882acd` additionally keeps incomplete policy-snapshot claims UNKNOWN and preserves the prior v2–v6 verifier archives alongside v7. Final acceptance candidate `7953ecdbd849c8afe0379bfec1b4a61133292c62` additionally runs every independent model seed in bounded workers and asserts the charter CI time budget; its shipped runtime bytes are unchanged from `8882acd`. Its implementation, live evidence, complete regression results and four-category open-item accounting are recorded in [capability addendum acceptance](capability-addendum.md). Positive signed queue acceptance must run on this exact candidate once its required App-policy read gate is satisfied within the authorized boundary; isolated selection is complete. No earlier signed transport or queue result substitutes for that final-candidate journey.
